1. run `pnpm lint`. do we have errors? YES -> fix them, NO -> continue
2. run `pnpm type-check`. do we have errors? YES -> fix them, NO -> continue
3. run `pnpm test`. do we have errors? YES -> fix them, NO -> continue
4. run `pnpm build`. do we have errors? YES -> fix them, NO -> continue
5. run `pnpm format`. do we have errors? YES -> fix them, NO -> continue
6. summarize the errors and the fixes